# Tech-DB 重构上下文

> 本文件记录重构前已经与项目所有者确认的产品和工程约束。在“共同理解已经达成”前，只更新本文档，不修改业务代码。

## 已确认的决定

1. **产品边界**：Tech-DB 是“静态技术情报门户 + 独立问答服务”。两者均为正式核心产品，但部署和代码边界必须分开。
2. **发布工作流**：继续采用单分支 `main`；所有修改必须在本地完整验证后，拆成清晰、可回退的 commit，再直接 push 到 `main`。不把未经验证的中间状态推送上线。
3. **目录重构**：允许系统性调整仓库目录，并同步修复所有引用路径。
4. **URL 兼容性**：所有现有公开网页 URL 必须保持不变；内部重构需保留兼容入口或跳转。
5. **唯一可信来源**：原始数据与生成脚本是内容的唯一可信来源。网页、JSON、离线页面等均为可再生成产物，后续 Agent 不应直接手改生成产物。
6. **生成产物入库范围**：只提交 GitHub Pages 部署和离线使用所必需的生成产物，并明确标注“禁止手改”；缓存、中间文件以及可重复生成且非部署必需的大文件不提交到 Git。
7. **问答服务部署形态**：问答服务必须同时支持个人电脑本地运行与公开 HTTPS 云端部署；GitHub Pages 前端可调用云端服务，本地开发环境也可独立联调。
8. **公开问答访问控制**：允许匿名访客使用，但必须实施防滥用和费用熔断。初始限流为单个 IP 每分钟 3 次、每天 30 次，全站每天 300 次，同时最多处理 3 个请求；超限返回 HTTP 429。管理员或开发测试密钥可绕过普通限流，达到每日费用预算后自动暂停问答服务。所有阈值必须可配置，不得写死在业务代码中。
9. **问答服务可移植性**：使用 Docker 打包问答服务，使其可在个人电脑本地运行，也可部署到支持容器和持久化存储的云服务器；暂不绑定单一云平台。
10. **问答可信性**：AI 只能依据 Tech-DB 数据回答，并标注资料来源；证据不足时必须明确说明“不确定”或“暂无资料”，不得自行编造或以模型自身知识补齐事实。
11. **视觉方向**：保持现有蓝白页面风格，只优化排版、间距、按钮、响应式布局和可读性，不进行大幅视觉改版。
12. **布局与导航**：允许在保持现有视觉基调及公开 URL 不变的前提下，重新整理导航、搜索区、数据卡片和 AI 问答入口。
13. **设备适配**：电脑端和手机端均需完整适配；手机端的导航、表格、搜索和问答功能必须可正常使用，不能只是按比例缩小桌面页面。
14. **无用文件处理**：重复、过时且经确认未使用的文件，可在相关测试通过后删除；如有需要，仍可从 Git 历史恢复。
15. **前端技术边界**：继续使用静态 HTML、CSS 和 JavaScript，不整体迁移到 React、Vue 等框架；仅进行结构化整理、代码优化和必要的测试建设。
16. **自动化质量检查**：增加统一的推送前检查流程，由 Agent 在直接 push 到 `main` 前自动运行；同时提供 Windows 一键检查入口，并在 push 后通过 GitHub Actions 再次在线检查。检查失败时不得推送未经验证的修改。
17. **离线使用边界**：数据库内容必须支持下载后离线浏览；AI 问答依赖公开云端服务，需要联网使用。离线时页面应保留数据库浏览能力，并对不可用的问答功能给出清楚提示。
18. **浏览器兼容范围**：电脑端和手机端均支持主流现代浏览器，包括较新版本的 Chrome、Edge、Firefox 和 Safari；不兼容已经淘汰的 Internet Explorer。
19. **密钥、模型与索引分发**：GLM API Key、管理员密钥及其他敏感凭据绝不上传 GitHub；本地 AI 模型和已生成的搜索索引不提交到 `main` 或 Git 历史，而是作为版本化附件放在 Tech-DB 仓库的 GitHub Releases 公共下载区。仓库必须提供自动下载、一键安装、版本匹配和文件完整性校验，使新环境能够获得正确的模型与索引并正常运行问答服务。
20. **完整迁移包**：在保留联网自动下载的同时，提供包含代码、固定版本模型和现成索引的完整离线迁移包；迁移包不包含任何 API Key，可通过 U 盘或局域网复制到新环境。
21. **Git 历史整理**：将执行时点七天以前的 `main` 历史压缩为一个保留当时完整文件快照的基础提交，保留最近七天的逐条提交，再强制更新远端 `main`。历史整理不得删除当前版本文件；已有克隆需要重新克隆。
22. **运行环境**：一键安装、启动和检查覆盖 Windows、Linux 与 Docker；三种环境使用同一份版本清单、Release 附件和完整性校验规则。

## 待确认

- 问答服务的具体云平台和可用性要求。
- 重构的验收标准与允许的功能变化范围。
